<?xml version="1.0" encoding="UTF-8"?>
<project>
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <relativePath>../org.assertj.eclipse.parent/pom.xml</relativePath>
    <groupId>org.assertj.eclipse</groupId>
    <artifactId>parent</artifactId>
    <version>1.0.0</version>
  </parent>

  <!-- plugins are given a different groupId to avoid conflict with features having the same artifactId -->
  <groupId>org.assertj.eclipse.plugins</groupId>
  <artifactId>org.assertj.eclipse.assertions.generator</artifactId>
  <packaging>eclipse-plugin</packaging>
  
  <name>${project.artifactId}</name>
</project> 
